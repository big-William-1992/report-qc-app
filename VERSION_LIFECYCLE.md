# 星衍放射质控软件 · 版本生命周期策略

> 版本：v1.0 | 2026-09-12

---

## 1. 版本发布策略

| 版本号 | 类型 | 发布频率 | 包含内容 |
|--------|------|----------|----------|
| x.y.0 | 功能版 | 每 1-2 个月 | 新功能、架构变更 |
| x.y.z | 修复版 | 按需 | Bug 修复、性能优化 |

## 2. 版本支持周期

| 状态 | 持续时间 | 提供内容 |
|------|----------|----------|
| 活跃支持 | 发布后 6 个月 | 全部 Bug 修复、安全更新、技术文档 |
| 仅安全更新 | 活跃期后 6 个月 | 仅安全相关修复，不含新功能 |
| 停止支持 | 安全期后 | 不再提供任何更新，建议升级 |

## 3. 升级路径

```
v4.x  →  v5.0（大版本）
  需要手动迁移，可能有 Breaking Changes
  开发者将提供迁移指南和数据转换脚本

v4.x → v4.y（小版本）
  自动更新，无缝升级
  数据库 schema 自动迁移
```

## 4. 旧版本处理

- 停止支持的旧版本仍可运行（无需联网验证）
- 但不再修复 Bug，可能存在已知问题
- 建议尽快升级到活跃版本

## 5. 自动更新

| 模式 | 配置 | 行为 |
|------|------|------|
| 在线更新 | 默认 | 检查 GitHub Releases，自动下载 |
| 离线更新 | `QC_UPDATE_LOCAL_DIR` | 从共享盘读取更新包 |

### 更新流程

1. 应用检查更新（启动时 + 每 24 小时）
2. 有新版本 → 弹窗提示
3. 用户确认 → 下载更新包
4. SHA-256 + Ed25519 签名验证
5. 验证通过 → 安装并重启

## 6. 安全更新

- 安全漏洞将在确认后 48 小时内发布修复版本
- 紧急漏洞可能跳过常规发布流程
- 安全公告通过应用内弹窗和公众号发布

## 7. 数据兼容

- 小版本升级（x.y.z）：数据库 schema 自动迁移，无需人工干预
- 大版本升级（x.0.0）：提供迁移脚本，需在升级前备份

---

**开发者承诺**：只要本软件仍在维护，停止支持的旧版本数据仍可正常读取。
