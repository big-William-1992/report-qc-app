# 星衍放射质控软件 · 数据安全白皮书

> 版本：v1.0 | 2026-09-12

---

## 1. 架构概述

```
┌─────────────────────────────────────────────────────┐
│                   放射科工作站                          │
│  ┌─────────────┐  ┌─────────────┐  ┌────────────┐  │
│  │  质控引擎    │  │  OCR 引擎   │  │  LLM 引擎  │  │
│  │  (本地规则)  │  │  (本地模型) │  │  (本地模型)│  │
│  └─────────────┘  └─────────────┘  └────────────┘  │
│                      │                               │
│                      ▼                               │
│              ┌──────────────┐                         │
│              │  SQLite 数据库 │  ← 所有数据存储位置    │
│              └──────────────┘                         │
└─────────────────────────────────────────────────────┘
```

**核心原则**：所有计算在本地完成，所有数据存储在本地。

## 2. 数据存储位置

| 平台 | 数据库路径 |
|------|-----------|
| macOS | `~/Library/Application Support/星衍放射质控软件/qc.db` |
| Windows | `%LOCALAPPDATA%\星衍放射质控软件\qc.db` |
| Linux | `~/.local/share/星衍放射质控软件/qc.db` |

## 3. 数据加密

| 层级 | 措施 |
|------|------|
| 文件系统 | SQLite 数据库文件权限 0600（仅当前用户可读） |
| 授权文件 | license.dat 权限 0600（仅当前用户可读） |
| 传输 | 本地回环通信（127.0.0.1），不经网络 |
| 备份 | 备份文件继承原文件权限 |

## 4. 网络通信

| 通信场景 | 是否必要 | 数据内容 |
|----------|----------|----------|
| 自动更新检查 | 可选 | 版本号（不含任何用户数据） |
| 离线更新 | 可选 | 更新包（从共享盘下载） |
| LLM API 调用 | 可选 | 报告文本（仅用户主动配置时） |

**默认行为**：软件完全离线运行，不发送任何网络请求。

## 5. 数据备份策略

- **自动备份**：每日一次，VACUUM INTO 在线备份（不锁库）
- **保留策略**：7 天 / 30 天 / 90 天三级轮转
- **备份内容**：数据库文件 + 配置文件（授权、规则、RIS 配置）
- **备份位置**：本地 `~/.local/share/xingyan_qc/backups/`

## 6. 数据导出

用户可随时通过以下端点导出数据：

```
GET /api/v1/export/data
```

返回所有样本、队列、设置数据（JSON 格式），供迁移或长期保存。

## 7. 数据删除

| 操作 | 方法 |
|------|------|
| 删除单条样本 | 应用内操作 |
| 删除备份文件 | 手动删除备份目录 |
| 卸载软件 | 删除数据目录 |
| 清空数据库 | 删除 qc.db 文件后重启 |

## 8. 安全审计

- **审计日志**：所有敏感操作（登录、密码修改、规则变更等）记录在审计日志
- **导出审计日志**：`GET /api/v1/admin/audit-logs/export?format=csv`
- **错误报告**：匿名错误上报，不含患者数据

---

**如有疑问，请联系 stardev@xingyan-ai.com**
