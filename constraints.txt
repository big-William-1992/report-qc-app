# 依赖版本锁定（2026-08-25 于 macOS + Win Py3.12 双端验证）
# 用法: pip install -r requirements.txt -c constraints.txt
# 策略: 只锁「出过事故」的核心链路版本; OCR 全家桶(rapidocr等)保持范围约束
# 原因: 2026-08-24 starlette 新版强制 httpx 导致 CI 连续失败, 此类漂移必须杜绝

fastapi==0.141.1
starlette==1.3.1          # TestClient 强依赖 httpx, 版本漂移重灾区
httpx==0.28.1             # starlette.testclient 必需(2026-08-25 CI 事故根因)
uvicorn==0.52.1
anyio==4.14.2
pydantic==2.13.4
pydantic-settings==2.14.2
SQLAlchemy==2.0.51
pywebview==6.2.1          # Windows 下 pythonnet/clr 组合仅对 6.x 验证过
pillow==12.3.0
numpy==2.4.6
cryptography==50.0.0
pypinyin==0.55.0
python-multipart==0.0.32
PyYAML==6.0.3
shapely==2.1.2
pyclipper==1.4.0
six==1.17.0
