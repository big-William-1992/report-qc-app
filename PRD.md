# 星衍放射质控软件 · 产品需求文档 (PRD)

> MetaGPT SOP · Phase 1 — Product Manager
> 版本：v4.3.6 · 更新日期：2026-09-13
> 状态：已发布，持续迭代

---

## 1. 产品定位

### 1.1 一句话定义

**面向放射科医生的影像报告智能质控助手**——粘贴即查、复制即控、后台快捷键一键质控、离线 OCR 自动回填、按错误严重度红/橙/蓝高亮、样本沉淀、科室报表、责任到人。

### 1.2 核心价值

| 维度 | 现状痛点 | 本软件解决方案 |
|------|---------|--------------|
| 效率 | 人工逐份核查，每份 5-10 分钟 | 一键质控，单份 < 3 秒 |
| 覆盖率 | 忙时漏检率高 | 剪贴板监听 + 后台快捷键，不遗漏任何一份 |
| 一致性 | 不同医生标准不统一 | 统一规则引擎 + 科室可配置 |
| 可追溯 | 纸质记录，难以量化 | 样本库 + 驾驶舱报表 + 责任到人 |
| 合规 | 依赖人工，无审计 | 审计日志 + 数据不出域 + 脱敏 |

### 1.3 商业模式

- **年费 ¥59/年**（单机授权）
- **浮动授权**：科室多机部署，按座位数计费
- **90 天免费试用**，试用期内功能完整
- **支付流程**：开发者生成激活码（Ed25519 离线签名），用户激活

---

## 2. 目标用户

### 2.1 用户画像

| 角色 | 场景 | 核心需求 |
|------|------|---------|
| 放射科主治医生 | 日常写报告 | 快速发现错别字、矛盾、遗漏 |
| 放射科主任 | 科室质控管理 | 报表统计、趋势分析、责任追踪 |
| 规培生 | 学习训练 | 样本库学习、误报了解 |
| 医院信息科 | 系统部署维护 | RIS/PACS 对接、批量处理 |

### 2.2 使用场景

```
场景 A：日常质控（80% 使用场景）
  医生写报告 → 复制到软件 → 1 秒质控 → 红/橙/蓝高亮 → 修正 → 复制回 PACS

场景 B：批量历史报告质控
  信息科配置 RIS 连接 → 批量拉取 → 全部质控 → 入库 → 导出质控报表

场景 C：OCR 辅助录入
  PACS 报告已打开 → 框选屏幕区域 → OCR 识别 → 回填 → 自动质控

场景 D：科室月度 QA
  主任查看驾驶舱 → 错误类型分布 → 每日趋势 → 导出 CSV 报表
```

---

## 3. 功能需求

### 3.1 核心功能（P0 — 必须）

#### F1：报告文本质控

**用户故事**：作为放射科医生，我希望粘贴报告文本后一键质控，以便快速发现错别字和逻辑矛盾。

**验收标准**：
- [ ] 粘贴报告文本后点击「运行质控」，3 秒内返回结果
- [ ] 结果列表按严重度排序（红 > 橙 > 蓝）
- [ ] 正文中用红/橙/蓝高亮标记问题位置
- [ ] 评分依据展示每项扣分的规则号与原因
- [ ] 覆盖 R1-R23 规则族（性别矛盾、左右混淆、评分缺失、单位错误、描述-结论矛盾、部位不符、内部矛盾、同音错别字、自定义互斥冲突、模板合规、信息框-正文矛盾、前后文/上下文逻辑错误、逐部位精确比对、检查完整性、繁体字检测、测量术语一致性、检查类型与描述匹配）

#### F2：剪贴板实时监听

**用户故事**：作为医生，我希望复制报告时自动触发质控，不必手动粘贴。

**验收标准**：
- [ ] 开启监听后，每 1 秒轮询系统剪贴板
- [ ] 复制 ≥15 字报告自动触发质控
- [ ] 命中问题时弹窗提醒（带严重度色条）
- [ ] 同一报告相似度 >90% 不重复弹窗
- [ ] 监听循环自愈合，异常不终止
- [ ] 支持「忽略」按钮，单次会话不再提示
- [ ] 支持「写入永久忽略规则」固化到配置文件

#### F3：后台全局快捷键

**用户故事**：作为医生，我希望即使焦点在 PACS 窗口，按快捷键也能触发质控。

**验收标准**：
- [ ] macOS/Linux 经 pynput 全局监听，Windows 经系统级热键
- [ ] 内置 0.5 秒时间戳防抖 + 执行中标记
- [ ] 未安装 pynput 或缺系统权限时降级为 SPA 内快捷键
- [ ] 快捷键在设置页可配置（默认 Ctrl+Alt+F9）

#### F4：离线 OCR 屏幕区域监控

**用户故事**：作为医生，我希望框选 PACS 屏幕区域，自动 OCR 识别并回填。

**验收标准**：
- [ ] 基于 RapidOCR 本地离线推理，不联网
- [ ] 框选区域 → 截图 → 灰度 + CLAHE 预处理 → OCR → 回填
- [ ] 内置变化检测，内容未变则跳过
- [ ] 模型权重内置 `assets/ocr_models`，无需下载

#### F5：自动修正预览

**用户故事**：作为医生，我希望同音错别字有预览确认，不自动改写矛盾类问题。

**验收标准**：
- [ ] 逐条列出同音错别字改动，如 `『姐姐』→『结节』`
- [ ] 每条可单独勾选 / 全选 / 全不选
- [ ] 仅 R8 错别字可自动修正
- [ ] 矛盾类问题（性别矛盾、左右混淆等）绝不自动改写，仅提示

#### F6：账号系统

**用户故事**：作为科室主任，我希望质控操作关联到具体医生，责任到人。

**验收标准**：
- [ ] 工号 + 密码登录（PBKDF2 600k 迭代）
- [ ] 首次启动引导创建管理员
- [ ] 密码策略：最少 8 位 + 字母数字组合 + 弱密码黑名单
- [ ] 登录锁定：失败 5 次锁定 15 分钟
- [ ] 审计日志：21 个敏感端点全覆盖

#### F7：样本库与驾驶舱

**用户故事**：作为科室主任，我希望查看科室质控趋势，导出月度报表。

**验收标准**：
- [ ] 样本列表：双击查看完整报告与质控结果
- [ ] 错误类型分布饼图 + 每日趋势折线图
- [ ] 导出 CSV（UTF-8-BOM，Excel 中文不乱码）
- [ ] 入库时可脱敏（去除患者姓名）
- [ ] doctor 角色仅见自己数据，admin 见全部

#### F8：授权系统

**用户故事**：作为开发者，我希望用户通过激活码激活软件，试用 90 天后需付费。

**验收标准**：
- [ ] 90 天免费试用，试用期功能完整
- [ ] Ed25519 离线激活码，不联网验证
- [ ] 激活绑定机器指纹，防复制
- [ ] 试用到期前 7/3/1 天告警
- [ ] 浮动授权：科室多机部署，按座位数计费

### 3.2 增强功能（P1 — 重要）

#### F9：RIS 直连

- 配置数据库连接（SQL Server / Oracle / MySQL / PostgreSQL）
- 填写拉取 SQL（须返回 `report_text` 列）
- 批量拉取报告质控入库
- RIS 轮询按 `interval_min` 调度

#### F10：规则维护

- 错别字词典增/删（即时生效，持久化到 `rules_config.json`）
- 互斥冲突增/删（词 A 与词 B 不应共存）
- 忽略名单固化
- 模板规范调整

#### F11：LLM 语义质控（可选增强）

- 通义千问 API 兼容 + 本地 ollama / llama.cpp
- 本地微调 LoRA adapter
- LLM 与规则引擎融合

#### F12：反馈闭环

- 误报/漏报 → 人工审核 → whitelist_delta 词表
- 用户反馈提交（问题/建议/咨询）
- 匿名错误报告

### 3.3 高级功能（P2 — 锦上添花）

#### F13：自动更新

- 检查更新 + 引导网页下载
- 离线更新通道（共享盘/U盘）
- 更新包签名验证

#### F14：数据导出

- 全量数据导出（样本+队列+设置 JSON）
- 供数据迁移

#### F15：审计日志

- 管理员可见的操作审计页面
- 按操作类型/工号/时间筛选
- 批量导出 JSON/CSV

---

## 4. 非功能需求

### 4.1 安全

| 要求 | 实现 |
|------|------|
| 数据不出域 | 所有数据仅存本机 SQLite，不上传网络 |
| 传输安全 | Bearer token（HMAC 签名），TTL 86400s |
| 密码安全 | PBKDF2 600k 迭代，版本化哈希 |
| 授权安全 | Ed25519 离线签名，机器指纹绑定 |
| 审计 | 21 个敏感端点审计日志 |
| 脱敏 | 错误报告/样本入库可脱敏 |
| 网络安全 | 非本机监听强制 `QC_API_SECRET` |

### 4.2 性能

| 指标 | 目标 | 实测 |
|------|------|------|
| 单份报告质控 | < 3 秒 | ~2ms（引擎）+ 网络开销 |
| 批量 50 份 | < 10 秒 | ~9ms（进程内单例） |
| OCR 识别 | < 5 秒/区域 | 变化检测跳过 |
| 数据库查询 | < 500ms | WAL + busy_timeout 30s |

### 4.3 合规

- **隐私**：PIPL 合规，数据仅存本机，不上传第三方
- **医疗**：辅助核查工具，不替代医师诊断
- **等保**：涉及二类医疗器械与等保三级相关合规要求
- **文档**：隐私政策、服务条款、数据安全白皮书、版本生命周期策略

### 4.4 兼容性

| 平台 | 最低版本 | 桌面壳 |
|------|---------|--------|
| macOS | 10.15+ | pywebview (WKWebView) |
| Windows | 11 | pywebview (WebView2) |
| Python | 3.10+ | — |

---

## 5. 规则引擎规格

### 5.1 规则族总览

| 规则 | 名称 | 严重度 | 严重度权重 |
|------|------|--------|-----------|
| R1 | 性别矛盾 | high | −30 |
| R2 | 左右侧混淆 | high | −30 |
| R3 | 评分标准缺失 | medium | −15 |
| R4 | 计量单位错误 | low | −5 |
| R5 | 描述-结论矛盾 | medium | −15 |
| R6 | 登记部位不符 | high | −30 |
| R7 | 描述内部矛盾 | — | 已合并入 R12 |
| R8 | 同音错别字 | medium | −15 |
| R9 | 自定义互斥冲突 | medium | −15 |
| R10 | 模板合规 | low | −5 |
| R12 | 句子前后文 | medium | −15 |
| R14 | 前后文（跨段） | medium | −15 |
| R15 | 上下文（段内） | medium | −15 |
| R16 | 随访时限（可选） | low | −5 |
| R17 | 逐部位精确比对 | high | −30 |
| R18 | 检查完整性 | medium | −15 |
| R19 | 形近字/读音候选 | medium/high | −15/−30 |
| R20 | 检查类型 | medium | −15 |
| R21 | 检查类型与描述匹配 | medium | −15 |
| R22 | 术语与测量一致性 | medium | −15 |
| R23 | 繁体字检测 | low | −5 |

### 5.2 评分系统

四个维度：**准确性 / 完整性 / 规范性 / 及时性**

- **准确性**：100 − Σ(严重度权重)，最低 0
- **完整性**：缺 R3 评分标准 → 80，否则 100
- **规范性**：存在问题 → 90，否则 100
- **及时性**：固定 100（预留维度）

---

## 6. 版本发布策略

### 6.1 版本支持

| 状态 | 期限 | 内容 |
|------|------|------|
| 活跃支持 | 6 个月 | 新功能 + Bug 修复 |
| 安全支持 | 6 个月 | 仅安全修复 |
| 归档 | 之后 | 不再更新 |

### 6.2 升级路径

- **v4.x → v4.x**：自动更新（检查更新 + 引导下载）
- **v4.x → v5.x**：手动迁移，可能有 breaking changes
- **数据库迁移**：启动时自动迁移，幂等

---

## 7. 已知限制

| 限制 | 说明 |
|------|------|
| 规则覆盖 | 规则引擎基于 NER + 规则匹配，非深度语义理解 |
| OCR 准确率 | 依赖截图质量，模糊/过小区域识别率下降 |
| 自动修正 | 仅 R8 错别字可自动修正，矛盾类绝不自动改 |
| LLM 依赖 | LLM 语义质控为可选增强，需额外部署 |
| 浏览器兼容 | 仅验证 Chrome/Edge WebView，未验证 Firefox/Safari |

---

## 8. 后续路线图

### v4.4.x（短期）
- [ ] Windows 安装包（GitHub Actions 构建）
- [ ] 公司官网 + 下载页
- [ ] 在线激活门户（替代手动生成激活码）
- [ ] 崩溃恢复（断点续传）

### v4.5.x（中期）
- [ ] 新模型接入（更多 LLM 后端）
- [ ] 报告模板检测增强
- [ ] 数据保留策略（定期清理）

### v5.0（远期）
- [ ] 多租户 SaaS 模式
- [ ] 自定义规则构建器（可视化）
- [ ] 国际化（i18n）
- [ ] 移动端支持
